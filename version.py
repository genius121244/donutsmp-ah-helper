"""
version.py
The one place the version number lives.

The updater compares this against the newest GitHub release, so bumping
this and tagging a release is the whole publishing process.
"""

VERSION = "1.2.0"

# Where the updater looks for new builds. Releases here must attach the
# built .exe as an asset for the in-app update to work.
GITHUB_REPO = "genius121244/donutsmp-ah-helper"
